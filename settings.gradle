dependencyResolutionManagement {
    versionCatalogs {
        testLibs {
            from(files('gradle/test-libs.versions.toml'))
        }
    }
}

rootProject.name = 'template-javafx-desktop'

include 'template-business', 'template-desktop'